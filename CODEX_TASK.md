# 鸣潮 FPS Unlock：本地 Windows 开发任务 v2

## 0. 本次交付目标与优先级

基于本目录 dev1 WPF 源码完成下一版实际实现、构建和验证，不从头换技术栈，不另做网页。先审计已有代码，修复编译问题，再落实下述需求。

最高优先级的新要求：

```text
主界面点击“开始游戏”
  ├─ 勾选“解锁帧率上限” -> 调用用户提供的 unlock.exe，由它启动/管理游戏及 FPS 解锁
  └─ 未勾选                -> 直接运行用户选择的原装 Shipping EXE
```

新应用本身不再加载/注入 FPS DLL，不再保留作为默认路线的 FpsSession 注入和独立 Named Pipe 控制，也不换成 SQLite 改帧。不要一边调用 unlock.exe，一边再启动 Shipping 或重复加载基础插件。外部解锁器自身的运行机制不需要重新实现。

本交接包中的代码是 dev1 原样基线，不是已实现上述变更的 dev2；旧 README、CODEX_HANDOFF 等与本任务冲突时以本任务为准。旧测试数量不代表新版本测试通过。

## 1. 工作区与执行权限

使用 E:\yanxin_ws\wuwa-fps-unlock，所有新工程内容放在该独立目录中。已有项目时先确认当前工作、git 状态和未提交更改，保护其内容，不用 clean/reset 覆盖用户修改。

开发采用 Windows 本机。可以在项目 .tools 中安装缺少的 .NET SDK，按项目固定版本恢复依赖；不要擅自启用 WSL/虚拟化或更换系统/驱动。桌面材料只读扫描和复制，不能移动、删除或修改原件。不要从用户桌面无关文件中读取个人资料。

先完成只读材料审计、代码更改、构建、假目录测试和原生 UI 验证。第一次写入真实游戏目录、执行解锁器或启动游戏前，用一次明确确认列出将做的操作与路径。用户确认后按计划完成测试；登录由用户处理。遇到权限/反作弊拦截就报告，不提供绕过，也不关闭安全软件。

## 2. 资料来源

本包已含：完整 dev1 源码、最终 UI 效果图、用户封面原图、头像原图、用户上传的“鸣潮.exe”和 ww_fps_config.ini。原 EXE 是用户 FPS 解锁器，不是鸣潮 Shipping。不要因为文件名是“鸣潮.exe”就拿它当游戏渲染 EXE。

桌面已存在素材目录，根部包含：

```text
素材目录\
  替换\
    [18 个 DLL；按实际内容审计]
  renodx-mfgunlock.addon64
  ReShade_Setup_6.8.0_Addon.exe
```

先解析 Windows 实际桌面路径（包括重定向情况），在限定深度内只读定位这些准确文件名。候选唯一时给出绝对路径并复制到 input\desktop-materials；存在多个候选时列明版本/哈希并集中询问一次。不要猜用户名、桌面文件夹名字或盘符。

截图不是二进制。实际文件版本、架构、大小、SHA-256 和存在的数字签名必须从文件读取；文件名和修改日期都不能证明版本或完整性。不要称用户本地哈希就是官方来源证明。

包内 input\unlocker\鸣潮.exe 未改动，可作为外部 unlock.exe 的源文件；已有桌面 unlock.exe 时先比较哈希，不默认为同一版本。复制经确认的程序及其所需同目录文件到 components\unlocker\，以 unlock.exe 为运行入口。保留原材料不变，记录别名、来源和哈希。

## 3. 前端定稿，不再重新设计

布局与外观采用 reference/APPROVED_UI.png。标题为“鸣潮 FPS Unlock”，副标题为“为40系显卡解锁帧率并提供多帧生成”。较薄的横幅、左图标和标题、右人物；上下留白紧凑，人物偏右。封面和头像直接使用 Assets 中的原文件，只允许裁剪、等比缩放、圆角和渐变遮罩。不重绘、不拉伸、不临时换图；标题文字必须原生渲染，不截整张效果图冒充界面。

主窗口较紧凑且偏横向，只显示 FPS 解锁开关、数值和滑块、设置、开始游戏。多帧生成、驱动门槛、ReShade、部署日志都不常驻主窗口。仅有异常时可以出现短状态提示，不增加导航、页签、宣传文案、账户和更新中心。

设置点击后打开独立的宽版窗口，复用同一实例，拥有路径、环境检测、FPS 设置、MFG 部署选择、Dynamic 条件状态、部署进度和日志。“开始部署”与“清除插件”两按钮等宽并铺满所在行；它们旁边不能再加重新检测按钮。只保留定稿中已有的交互，不无故添加新页面。

两个窗口共用状态，不各自缓存一套 FPS。关闭设置不退出主窗口、不结束游戏或解锁器。使用 Grid 自适应、异步操作和真实进度；不同 DPI 下标题、输入框、按钮与长路径不能裁切。

## 4. 启动路由必须实现为可测试逻辑

建议增加 LaunchPlanBuilder、ExternalUnlockerService、UnlockerConfigAdapter，或等价职责模块。分别保存：

- GameRoot：用户手动指定的游戏安装根目录。
- ShippingExePath：用户手动指定的真实、原装 Shipping 可执行文件。
- UnlockerExePath：应用管理的 components\unlocker\unlock.exe，不能与 Shipping 混为同一输入。
- UnlockerWorkingDirectory、UnlockerConfigPath：根据该版本实际读取行为确定。
- FPS 开关、目标 FPS，以及外部工具所需的已验证启动模式。

不把当前 INI 里根目录的 Wuthering Waves.exe 路径直接当成 Shipping 的证明。允许在内部适配解锁器所需的官方入口路径，但未勾选分支必须直接走用户确认的 Shipping，不经过外部解锁器/官方启动器。ReShade 的安装目标也必须依据真实渲染 EXE，而不是本应用或 unlock.exe。

### FPS 开启分支

先检查 unlock.exe 与依赖、配置可写、游戏是否已运行和同一版本解锁器是否已在运行。更新本应用管理的解锁器配置，再用绝对路径与正确 WorkingDirectory 启动该外部程序。参数用安全的参数数组，不拼 cmd 命令。

不要虚构 `--fps`、`--game`、`--headless` 等命令行参数。先核对用户实际程序的配置/自动启动接口、公开源码对应关系或静态行为；不能仅因项目同名就认定二进制对应某次提交。

上传 INI 当前字段包括 FpsValue=240、PathValue、DX11Enabled=False、AutoStartEnabled=False、GameLaunchExe=0、GameServerArea=2 等。外部工具自动启动所需设置应在确认语义后由适配器维护：设置 FpsValue；更新它实际接受的启动路径；通常需要启用其现有 AutoStart 功能，但不能在未验证的情况下把某个字段等同于 headless；GameLaunchExe/ServerArea 等枚举不要猜 0/1 含义。

INI 保留未知键，不整体重置；只改控制所需键。不要启用 FOV、隐藏 UID、去模糊、DX11、省电、进程优先级等无关功能。原材料配置不改动，只改运行副本。

不要把“运行 unlock.exe”视作已经启动游戏。监测真实目标进程/路径/PID与窗口，设合理超时；不要无限等待外部解锁器退出，因为它可能常驻管理游戏。外部工具自身能自动启动/最小化时复用其能力；CreateNoWindow 不应被当作保证隐藏 WPF 窗口的办法。若原工具仍有不可消除的弹窗/托盘，明确报告而不是伪称只有两个窗口，也不要静默改造成另一套注入实现。

FPS 分支失败时报告路径、阶段和原因；不静默退回原装启动，以免用户误认为已解锁。需普通启动时要求用户明确关闭开关或确认。

### FPS 关闭分支

只运行 ShippingExePath。不得启动 unlock.exe、加载/注入 FPS DLL、写外部解锁器运行设置、改 SQLite 或读取/写入游戏内存。不得卸载或禁用已经部署的 MFG/ReShade；FPS 开关不控制 MFG。

游戏已运行时不要再开第二实例；按钮显示运行中或聚焦已经确认的目标窗口。运行中改变 FPS 启停设置只在下一次启动生效，不能宣称撤销当前游戏内插件。数字/滑块修改：除非外部工具有已验证可调用的实时接口，否则保存为下次启动值，显示“下次启动生效”，不要声称修改 INI 就已实时调整。

### 两个分支共通

防双击重复启动；状态区分“启动工具”“等待游戏”“游戏运行中”“失败”。关闭本应用不擅自杀游戏或外部工具；不结束不是本应用创建且已确认的进程。需要的权限提示如实交给 UAC，不绕过。所有新流程可用 mock/fake 外部进程做无游戏副作用测试。

## 5. 桌面 DLL 包与清单适配

用户已确定整套替换文件，不在线重新挑版本或强行拆 Fixed/Dynamic 两套包。保留整套实际输入；审计每个文件，再建立明确源 -> 目标映射。

截图显示的 18 个文件见 input/desktop-materials/EXPECTED_FILES.md。旧脚本和 SafePaths 白名单缺少 5 个：NvLowLatencyVk.dll、nvngx_deepdvc.dll、nvngx_dlssnr.dll、sl.dlss_nr.dll、sl.nvperf.dll。同步修订所有校验位置、模型和测试，不能只改一个脚本，也不能用允许任意 DLL 的方式绕过校验。

“替换”目前是平铺源文件夹，不是相对于游戏根的目录树。不能直接把该目录交给旧 Prepare-Payload.ps1 就认定会安装正确，更不能把“替换”文件夹本身复制进游戏根目录。

只读检查用户游戏中已存在的 NVIDIA/Streamline 目录、官方目录布局和可获得的成功运行日志。每项固定目的锚点与相对路径；多份同名 DLL 不能全盘覆盖；确需更新多个位置须逐项记录根据。包内新增且游戏中未出现同名文件的，需明确部署位置依据，不能默默丢弃，也不能一律塞到 Shipping 旁边。有歧义时集中询问精确目标目录。

输出经确认的 manifest.json / payload-map.json、每文件版本/长度/SHA-256 和身份检查结果。先验证全部输入及目标，再执行任何替换；失败留下真实逐项记录。重复部署跳过相同文件。不因为某 DLL 名字带 Vk 就自行剔除，也不因为包里有它就推断 DX12 游戏会加载它。

## 6. ReShade 本体自动安装

优先使用用户提供的 ReShade_Setup_6.8.0_Addon.exe，经检测和哈希登记后调用，不重新下载另一个版本。不 fork、不精简官方 ReShade，不附加着色器/滤镜包。

已有兼容 Full Add-on Runtime 优先复用；普通/旧版若需升级先确认，保留现有配置、滤镜和其他 addon。检测到非 ReShade 的代理、多份 Runtime 或无法解析的配置路径时停止，不能强制覆盖。保留用户原 AddonPath 与其他配置，只合并本工具负责的键和 early-load 列表项。

根据实际 6.8.0 Setup 行为实现 headless 首装/更新，具体代理参数由用户成功布局或实机验证锁定；不要把游戏走 D3D12 与代理文件一定叫 d3d12.dll 混为一谈。读取退出码后还要验证实际生成文件和路径，不能只看到进程退出就判成功。不要承诺 headless 必定没有网络访问；官方 Setup 可能有兼容表等请求，受控离线场景另测。

目前 ReShadeService 主要从官方地址下载 Setup，需新增本地来源优先；本地源完整有效时不能为了重复下载而阻断部署。写入或升级权限按实际需要申请，不使主界面长期管理员运行。已有共享/重定向 AddonPath无法安全自动处理时明确说明，不能破坏用户已有加载路径。

## 7. MFG 能力判定和日常使用

FPS 上限解除与 MFG 部署相互独立。只开 FPS 不安装 ReShade、不替换 NVIDIA 文件，也不以 RTX 40/Dynamic 条件阻断它。MFG 安装后没有必要在每次启动时重装；首次部署与日常开始游戏分离。

Fixed 与 Dynamic 不是互斥安装包；595.41 只用于当前 Dynamic 驱动预检查，不用于 Fixed 的统一门槛。未知驱动显示未知，Fixed 缺少统一最低版本不等于所有旧驱动保证可用。记录这套实际包的验证范围；包本身有另行证明的条件则如实处理。

驱动达到门槛只显示“Dynamic 驱动条件通过，游戏内待确认”。不得靠硬件名称/文件名/文件已拷贝直接显示 Dynamic 已启用。保留实际 GPU 枚举和 HAGS 配置状态的准确措辞，不硬编码截图中的 596.60、RTX 4080 和 Windows 版本。

主窗口启动前只作快速完整性检查。文件被官方更新/修复改变时提示到设置检查；默认不静默把老包覆盖到新游戏。不能断言官方启动必然清除所有第三方插件。MFG 选择关闭只是下次部署不选择它，不是卸载。

## 8. 清除插件与用户文件保护

不备份被替换的原游戏 DLL，也不加一键恢复原版。可以维护部署清单/所有权/本工具修改过的配置键记录，不能把记录称作原游戏备份。

清除前要求游戏关闭、显示具体对象并确认。仅清理本工具新建、当前仍与登记匹配的 MFG addon，以及本工具所有且未被他人更改的配置项；保留其他 early-load 列表项。不批量删除 *.dll、*.addon64 或整个目录。

默认保留用户 ReShade、preset、shader、其他 addon、所有 NVIDIA DLL、原装 Shipping、外部 unlock.exe。没有原 DLL 时不能靠删除 NVIDIA 文件假装恢复。不存在所有权记录或哈希变化时跳过/警告，不能猜测。部分失败留下报告，不声称完整回滚。

## 9. 旧代码迁移重点

核查 AppViewModel 的启动入口、FpsSession 字段和调用、AppPaths.Plugin、发布项目对 ww_plugin_base.dll 的引用、退出/定时监控逻辑，以及所有测试/说明。默认运行和发布不能仍然要求本工具自带 FPS 基础 DLL；旧组件可移到归档，但不参与新启动路径。

核查 ReShadeService.DownloadSetupAsync 的本地输入优先、准备脚本的白名单与平铺映射、SafePaths 的同步规则。不要只把按钮文字改了、底层仍走旧流程。

在导入用户资产后，重新生成来源记录与哈希清单。每项测试写明原版本和新版本，不把旧快照冒充当前运行结果。更新 README 与 docs/CODEX_HANDOFF.md，并注明新外部启动路线覆盖旧方案。

## 10. 必须完成的验收

先实际编译 WPF 并运行核心回归。原生窗口能打开是基本门槛，HTML 预览不算 WPF 验收。测试包括：

1. FPS 开只调用 unlock.exe，FPS 关只调用选定 Shipping；没有隐藏的第二个启动或本应用注入调用。
2. 正确 INI/工作目录/目标路径/枚举适配，首次自动启动可控；缺程序、坏配置和超时准确报错。
3. 已有解锁器/游戏、防双击、不同路径的同名进程、用户取消、程序关闭、游戏退出等状态可恢复。
4. 中文/空格/长路径，所有参数正确转义；文件不存在不创建假成功。
5. 18 个实际 DLL 的显式映射、重复安装、无目标新增、多目标歧义、源哈希损坏、目标占用、写入中断。
6. ReShade 无安装、可复用、普通版需确认升级、未知代理阻断、AddonPath 保留、无网络、本地 Setup 损坏、UAC 取消。
7. 两窗口值同步、反复打开设置、禁用态、合理进度、150%/200% DPI、封面裁剪、等宽按钮、日志不阻塞 UI。
8. 清除仅移除所有权明确的本工具插件，不伤原游戏/用户 ReShade；重复清除可预期。
9. MFG 文件已部署与真实运行能力分别验证；Fixed 不误用 Dynamic 门槛；状态未知不能被画为成功。

先在假目录与 fake launcher 中验证分支和写入，再经确认对当前鸣潮做真实部署与启动。必须明确区分“构建通过”“原生界面通过”“文件部署通过”“游戏启动成功”“FPS/MFG 游戏内实测”。未做的项目写待测。

## 11. 交付

交付当前源码、Windows x64 便携发布目录/ZIP、主 EXE 与配套文件哈希、依赖/许可/原始文件来源、构建和测试日志、真实 WPF 主/设置窗口截图、材料映射清单及必要的启动验证记录。发布程序按其真实依赖打包，不强求单 EXE。不得公开上传用户本地二进制或发布到远端，公开分发权限单独审查。

在 E:\yanxin_ws\wuwa-fps-unlock\artifacts 下输出，并报告准确路径和仍未闭合事项。无法实机时也继续完成可做的代码、编译和假目录测试，不把未执行的游戏效果写成功。不要最后只返回一份设计方案。
