# 2026-09-14 用户操作的真实游戏测试

用户反馈：无可见报错、帧率解锁成功、Dynamic看起来正常。原截图保存在user-evidence目录。

- 文件部署：真实目录18DLL和addon已同哈希，工具校验并复用；合并MFG INI，未虚构本轮DLL写入。
- 外部启动：11:39:42标准UAC worker记录启动unlock.exe PID29816，11:40:14确认Shipping PID4172渲染窗口；11:43:16记录游戏退出。
- Dynamic日志：11:40:59开始多次accepted target160，报告2至6实际presentations；运行时报告支持。属于本轮日志，非此前历史证据。
- 实际效果：用户确认解锁且运行正常；截图计数约93–96 FPS，未证明稳定160最终输出或独立测量的生成倍率。截图也出现waiting for next SetOptions/运行策略未到达slInit提示，与曾accepted日志分别保留，不扩大结论。
- 无可见报错不等于日志零警告：保留D3D引用计数不一致及退出时Add-ons are still loaded警告。

## 清除前停止点
进程检查未发现Shipping/unlock/nvngx_update。Shipping、18DLL、ReShade本体、addon哈希均无变化。但ReShade.ini中的ForceMultiplier由本工具写入0变为4，当前INI SHA256=3b30ca1cf33ba950aa0be87a8c4fb075ba8961d85ba51e047e01fc9cb2f49a08。
按用户“执行前变化先停止报告”的要求，尚未清除或再次启动。若确认继续，应保留被用户/运行时改为4的配置，不强行还原；撤销其他仍归本工具负责的键及加入的LoadFromDllMain项，删除已确认映射的18DLL与addon，再由用户验证下一次普通启动和自动补齐。自动补齐/官方校验恢复仍未执行。

## 用户报告已清除后的只读复核
工具日志12:53:44记录19项删除，无变化保留；当前addon不存在，18DLL中8个不存在、10个在12:58:52至12:59:01重新创建，且全部为不同于用户材料的SHA256（Streamline2.10.0、DLSS310.5/310.6）。不能把重新出现误报为删除失败，也不能在不知道触发方式时声称普通Shipping自动补齐成功。
ReShade dxgi.dll仍存在且原哈希不变。Enabled、RuntimeSelectionMode及本工具加入LoadFromDllMain项已撤销；原有DynamicMFG=1及后改ForceMultiplier=4保留。其他滤镜/未知文件没有完整前后目录快照，不能仅凭本体校验声称逐文件无变化。
等待用户说明是否直接启动、官方启动器自动修复或手动校验，以及是否正常进入游戏。当前没有继续删除或重部署。

## 用户最终确认：官方校验恢复
用户明确说明：经过官方文件校验，修复后可以进入游戏，帧率解锁和多帧生成都没了。

验收结论：
- 清除文件操作：工具日志记录删除18DLL+addon共19项；后续只读核对addon缺失，ReShade本体原哈希保持。
- 清除配置：本工具新增Enabled、RuntimeSelectionMode、LoadFromDllMain项已撤销；原有DynamicMFG=1及用户后来修改ForceMultiplier=4保留。
- 官方校验恢复：用户已实际执行，确认修复后可进入游戏；10个不同版本/哈希DLL重新出现与此操作对应。
- 普通启动自动补齐：未取得独立成功证据，不能标通过。已走官方校验恢复，不能用恢复后的正常启动反证自动补齐成功，也不能断言此前普通启动必然失败。
- 修复后效果：用户报告帧率解锁与多帧生成均不再生效；本条为用户实测反馈，不推断清除动作删除了外部unlock.exe或改变其实现。
- 整体清除/自动恢复验收：文件清除与官方修复可用；无需官方校验的自动恢复要求未通过验收。产品不得承诺删除后游戏会自行补齐。
