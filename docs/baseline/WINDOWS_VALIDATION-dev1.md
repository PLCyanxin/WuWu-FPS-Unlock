# Windows 与鸣潮验收清单

状态：**全部原生项目待执行**。本文件不是测试通过报告。请逐项记录 Windows build、游戏版本、GPU、驱动、EXE 哈希、FPS DLL 哈希、载荷哈希和 ReShade 代理/版本。使用普通权限启动 UI；不要关闭安全软件或反作弊。

## 1. 构建

- [ ] 在 `E:\yanxin_ws\wuwa-fps-unlock` 独立工程中运行 Build.cmd。
- [ ] `tests.log` 所有核心用例通过，失败数为 0；不要只看最后一行发布输出。
- [ ] `build.log` 原生 WPF 编译成功，记录警告并审查；目前尚无编译结果。
- [ ] 发布文件包含主 EXE、components/ww_plugin_base.dll、licenses；插件 SHA256 与 PROVENANCE 一致。
- [ ] 无 .NET runtime 的干净 Windows 环境可以运行自包含输出。
- [ ] 程序未被“设置图片/HTML”冒充；截图必须来自实际 WPF 窗口。

## 2. 界面和状态

- [ ] 默认只开紧凑主窗口；设置打开独立大窗口，再次点击复用同一窗口，最小化后可唤回。
- [ ] 两个窗口的 FPS 开关、数值和滑块同步；数值输入、失焦、鼠标拖动及键盘方向键都有效。
- [ ] FPS 接受 30–420 整数；空值、负数、超限、非数字不能触发启动或发送。
- [ ] 主界面没有 MFG 开关、硬件列表或日志面板。
- [ ] 原头像和原封面未重绘，横幅更薄，角色靠右，文字不遮关键脸部。
- [ ] 部署/清除按钮等宽充满同列，右侧不留多余空白；不出现邻接重新检测按钮。
- [ ] 100%、125%、150%、200% DPI、多显示器不同缩放、中文长路径下检查截断和换行；必要时修正 XAML，不凭 HTML 检查代替。
- [ ] 两窗口忙碌状态同步，不能并发部署/清除/启动；不能在写文件中途关闭窗口。
- [ ] 出错具有可理解的提示，日志能滚动、清空；没有虚假进度百分比。

## 3. 路径和原生 FPS 功能

- [ ] 手选游戏根目录和真正渲染 EXE，不以旧 INI 或窗口示意中的路径作为保证。
- [ ] 错选工具 EXE/launcher/32-bit 文件/根目录外 EXE 时阻断。
- [ ] 仅开 FPS 功能时，不需要 MFG 包，不动 ReShade/DLSS/Streamline。
- [ ] 正常启动后找到正确 UnrealWindow 与 PID；无法读取目标进程时应明确失败，不采取绕过。
- [ ] 原 DLL 实际可加载；管道服务端 PID 确实属于该游戏，FPS 指令发送成功。
- [ ] 用可信游戏内/呈现监测方式验证设置上限的实际效果；区分原始渲染帧率与插帧输出帧率。
- [ ] 240→180→240 实时修改，切图/切场景后行为可接受，记录负载/稳定性，不把目标数字当成实测值。
- [ ] 与旧解锁器同时运行时拒绝冲突，不重复加载另一套 FPS DLL。
- [ ] 游戏退出后可以再次启动；关闭本工具不会误杀游戏。
- [ ] 不支持热卸载时，运行中 FPS 启停开关保持锁定并说明需要重启。

## 4. 显卡与驱动

- [ ] 核显+独显环境仍正确枚举 NVIDIA；没有 NVIDIA/驱动接口失败显示未知。
- [ ] 591.86 或低于 595.41 的案例只限制 Dynamic，不误禁 FPS-only，也不声称 Fixed 已实测。
- [ ] 595.41 边界和更高驱动显示“驱动条件通过，游戏内能力待确认”。
- [ ] HAGS 缺省值显示默认/未知，配置开启不伪装成运行态确认；不会自动改系统或重启。
- [ ] 多 GPU 时确认游戏实际渲染 GPU，不只看 NVAPI 中第一张卡。

## 5. ReShade 自动化与保留既有环境

测试前使用独立实验副本并自行安排必要的备份；产品按要求不提供原游戏文件备份。

- [ ] 从官方 HTTPS 正常下载 Full Add-on Setup 6.8.0，失败时没有假报安装成功。
- [ ] 包内配置的 `dxgi`/`d3d12` 与已验证布局相符；不通过 API 名猜测。
- [ ] 首装、重复部署、复用、需要确认的升级四条路径分别测试。
- [ ] 普通 ReShade 需用户确认升级，不 silently 覆盖；未知图形代理必定停止。
- [ ] 既有 preset、shader、texture、快捷键、addon 和 INI 无关章节保留。
- [ ] 自定义游戏内 AddonPath（含 `.` 和相对路径）正确；游戏外共享 AddonPath 明确拒绝、不写入。
- [ ] Setup 退出后实际 runtime 路径、x64/Full Add-on 特征、可选 SHA 校验正确。
- [ ] 不安装任何额外滤镜包，不同时加载两份 ReShade。
- [ ] UAC 取消、下载中断、无法写入、游戏占用、磁盘不足时状态和日志准确。

## 6. MFG 文件和运行态

- [ ] 接收真正的 MFG/DLSS/Streamline 文件包，并对 manifest 的每个目标路径人工复核。
- [ ] 全部源文件哈希错误时，在首次覆盖前停止。
- [ ] 运行库整套一致，没有从“latest”混搭未知文件；包内默认画质配置来源明确。
- [ ] Fixed/Dynamic 共用组件，按硬件驱动条件配置，不做互斥安装包误导。
- [ ] Full Add-on 实际加载 `renodx-mfgunlock.addon64`，不是仅目录中有文件。
- [ ] 游戏内 FG 设置、实际加载的 DLSS-G/Streamline、NVIDIA App override/OTA 是否介入均核对。
- [ ] Dynamic capability 和实际接受动态模式的状态按 addon / runtime 诊断确认，不能仅凭驱动达标。
- [ ] FPS-only、MFG-only、两者同时、两者均不启用四种组合分别测试；不以单项成功推论联合成功。
- [ ] 启动前后文件哈希和更新行为有记录；游戏修复或更新后，工具提示检查，不静默覆盖旧包。

## 7. 清除、失败和重试

- [ ] 清除只删除本工具创建、哈希未被他人改变的标准 MFG addon。
- [ ] 原有用户 addon、被用户修改的 addon、ReShade 和 NVIDIA DLL 均保留。
- [ ] 本工具改过的 INI 键可按记录撤销，不删除其他 early-load 项或用户后续修改。
- [ ] 清除后重新部署开启新的所有权周期，不把这期间别人安装的文件认领为本工具新建。
- [ ] 第 N 个文件故障时记录 PartialFailure，不声称整体回滚；下次启动要求先处理。
- [ ] 上次部署异常、再次尝试同一包、插件被人工删除、包目录移动等情况可理解地反馈。

## 发布门槛

编译通过、HTML 预览通过和游戏验证是三份不同证据。每个测试记录执行人、时间、版本与日志。不宣传“100% 不冲突/不封号”。公开分发前补齐许可证与原始素材、NVIDIA DLL 和第三方二进制的分发授权核查；不要把所有文件套用一个 MIT 许可。
